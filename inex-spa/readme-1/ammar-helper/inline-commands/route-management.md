# Route Management

```bash
# Create a route
php ammar make:route -1 routeName -2 isDynamic -3 requestType -4 isAPI
# Example: php ammar make:route -1 blog -2 yes -3 GET -4 no

# Delete a route
php ammar delete:route -1 routeName
# Example: php ammar delete:route -1 blog
```
